import styled from "styled-components";
import * as common from "../../common";

export const flag = styled(common.flexRow)``;
export const name = styled(common.flexRow)``;
export const uiRates = styled(common.flexRow)`
  width: 80vw;
  margin-top: 5px;
  padding: 5px;
  justify-content: space-between;
  height: 80px;
`;
